{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=process.argv[1]||'';if(p.endsWith('.js')||p.endsWith('.jsx')){const f=p.replace(/\\.js$/,'.web.js');const fs=require('fs');if(!p.includes('.web.')&&fs.existsSync(f)){console.log('⚠️  REMINDER: Web counterpart exists: '+f+' — make sure to update it too!')}}\" -- \"$CLAUDE_FILE_PATH\""
          }
        ]
      }
    ]
  }
}
