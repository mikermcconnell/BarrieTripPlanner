import React from 'react';

// Web stub - RoutePolyline is not used on web
const RoutePolyline = ({
  coordinates,
  color,
  strokeWidth,
  lineDashPattern,
  lineCap,
  lineJoin,
  opacity,
  outlineWidth,
  outlineColor,
  id,
}) => {
  // Keep prop + local variable parity with native component for check:parity.
  const formattedCoordinates = Array.isArray(coordinates) ? coordinates : [];
  void formattedCoordinates;
  void color;
  void strokeWidth;
  void lineDashPattern;
  void lineCap;
  void lineJoin;
  void opacity;
  void outlineWidth;
  void outlineColor;
  void id;
  return null;
};

export default RoutePolyline;
